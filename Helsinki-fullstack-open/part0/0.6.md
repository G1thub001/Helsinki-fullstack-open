```mermaid
sequenceDiagram
    participant browser
    participant server

    browser-->>browser: User submits form, JavaScript intercepts submission
    browser->>server: HTTP POST /new_note_spa
    server-->>browser: HTTP 201 Created
    browser-->>browser: Update notes list and re-render page
```
